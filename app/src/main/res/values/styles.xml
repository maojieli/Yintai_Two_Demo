<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="MaterialIndicator.Demo">
        <item name="mi_indicatorColor">#99ffff</item>
        <item name="mi_indicatorPadding">32dp</item> <!--指示点间距-->
        <item name="mi_indicatorRadius">4dp</item><!--半径-->
    </style>

    <style name="MaterialIndicator">
        <item name="mi_indicatorRadius">4dp</item><!--半径-->
        <item name="mi_indicatorColor">#009688</item>  <!--指示点被选中的颜色-->
    </style>
    <style name="pop_main">
        <item name="android:windowEnterAnimation">@anim/pop_in</item>
        <item name="android:windowExitAnimation">@anim/pop_out</item>
    </style>
    <!-- 购买弹出动画 -->
    <style name="PopupAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popu_up</item>
        <item name="android:windowExitAnimation">@anim/popu_out</item>
    </style>

</resources>
